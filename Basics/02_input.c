// Write a program in C to take input from the user and display it.
#include <stdio.h>

int main()
{
    int a;
    printf("Enter the number: ");
    scanf("%d", &a);

    printf("You have entered: %d\n", a);

    return 0;
}